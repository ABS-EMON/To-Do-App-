version: "3.9"

services:
  todo-app:
    build: .
    container_name: todo_app
    ports:
      - "5000:5000"
    volumes:
      - todo_data:/app/todos.json
    restart: unless-stopped

volumes:
  todo_data:
